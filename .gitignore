# Segredos: os .env de verdade nunca entram aqui.
# Os *.example são o que fica versionado.
.env
**/.env
backup.env
*.key
*.pem
*.p12

# Dumps e artefatos de backup
*.sql
*.sql.gz
*.dump

# Ruído do editor
.DS_Store
*.swp
*~

# OCIDs da tenancy (o .example é o que fica versionado)
scripts/oci.env

# Artefatos de execução do retry loop
criar-instancia.log
.criar-instancia.lock

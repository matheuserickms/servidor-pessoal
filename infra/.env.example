# =============================================================================
# Destino no servidor: /opt/stacks/_infra/.env   (chmod 600)
# Copie de .env.example, preencha, e NUNCA comite o .env.
# =============================================================================

# Seu domínio raiz, sem https:// e sem barra final.
DOMINIO=exemplo.com.br

# E-mail para o Let's Encrypt: recebe aviso se a renovação falhar.
ACME_EMAIL=matheus.erick.97@gmail.com

# Senha do superusuário do Postgres.
# Gere com:  openssl rand -base64 32
POSTGRES_PASSWORD=
